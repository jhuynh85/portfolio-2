import React from "react";
import styles from "./Jumbotron.module.css";
import CSSModules from "react-css-modules";

const Jumbotron = () => (
  <div className="parallax">
  
  </div>
);

export default CSSModules(Jumbotron, styles);